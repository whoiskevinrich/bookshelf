# Notes / local task scratchpad
notes
todo/

# Claude Code worktrees (ephemeral, managed by Claude Code)
.claude/worktrees/

# Dependencies
node_modules/

# Build outputs
dist/
cdk.out/
# CDK context cache — may contain resolved SSM values (secrets)
cdk.context.json

# Environment — never commit secrets
.env
.env.local
.env.*.local
.env.mock

# Playwright
.auth/
playwright-report/
test-results/
blob-report/

# OS
.DS_Store
Thumbs.db
/.claude/settings.local.json

# Cognito migration exports — contain user emails/subs (see runbooks/cognito-email-mutable-migration.md)
/migration/
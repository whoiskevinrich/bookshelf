# Mock mode — API calls intercepted by MSW; no API server or DynamoDB needed.
# Auth is REAL Cognito — copy Cognito values from apps/web/.env.local (run scripts/worktree-setup.ps1).
VITE_MOCK_API=true
VITE_API_BASE_URL=

{
  "name": "@bookshelf/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "type-check": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:smoke": "vitest run --config vitest.smoke.config.ts",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "validate:covers": "node --import tsx/esm scripts/validate-covers.ts",
    "migrate:isbn13": "node --import tsx/esm scripts/migrate-isbn10-to-13.ts",
    "migrate:attributes": "node --import tsx/esm scripts/migrate-status-to-attributes.ts",
    "test:coverage": "vitest run --coverage",
    "dev": "tsx watch src/server.ts",
    "db:seed": "node --env-file=.env.local --import tsx/esm scripts/seed-local.ts"
  },
  "dependencies": {
    "@aws-lambda-powertools/parameters": "^2.33.1",
    "@aws-sdk/client-cognito-identity-provider": "^3.1075.0",
    "@aws-sdk/client-dynamodb": "^3.1075.0",
    "@aws-sdk/client-rekognition": "^3.1075.0",
    "@aws-sdk/client-ssm": "^3.1075.0",
    "@aws-sdk/lib-dynamodb": "^3.1075.0",
    "@hono/node-server": "^2.0.6",
    "hono": "^4.12.27",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.162",
    "@types/node": "^26.0.0",
    "dotenv": "^17.4.2",
    "esbuild": "^0.28.1",
    "kill-port": "^2.0.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
